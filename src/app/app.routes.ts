import { Routes } from '@angular/router';
import { HeroesPageComponent } from './heroes/pages/heroes-page/heroes-page.component';

export const routes: Routes = [
    {path: '', component:HeroesPageComponent}
];
